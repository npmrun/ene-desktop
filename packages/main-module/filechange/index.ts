import watcher from "@parcel/watcher"

export function init() {
    // logger.debug("开始监听")
    // ;(async () => {
    //     // 使用vscode编辑保存会出现两条更新记录，删除创建都只出现一条。
    //     let subscription = await watcher.subscribe("D:\\1XYX\\pro\\electron-template\\build-assets", (err, events) => {
    //         if (err) {
    //             logger.error(err)
    //             return
    //         }
    //         logger.debug(events)
    //     })
    // })()
}
